{
  "name": "3ptracer",
  "version": "1.0.0", 
  "description": "Advanced Third-Party Service Discovery Tool - Cloudflare Pages deployment",
  "main": "index.html",
  "scripts": {
    "build": "npm run copy-to-dist",
    "copy-to-dist": "mkdir -p dist && cp -r *.html *.css *.js dist/ && echo 'Build completed - files copied to dist/'",
    "preview": "npx serve dist",
    "dev": "npx serve .",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy dist --project-name=3ptracer",
    "deploy": "npm run build && npm run deploy:worker && npm run deploy:pages"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kedster/3ptracer.git"
  },
  "keywords": [
    "dns",
    "security",
    "third-party",
    "cloudflare",
    "web-security"
  ],
  "author": "Cyfinoid Research",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0",
    "serve": "^14.0.0"
  }
}